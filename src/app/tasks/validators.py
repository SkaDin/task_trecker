# from src.app.tasks.schemas import TaskCreate
#
#
# def check_duplicate_task_title(data: TaskCreate):
#     if self.get_one(data["title"]) is not None:
#         raise HTTPException(status_code=409, detail="Task with this title already exists", )
